name: ev_stat
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.1.3
  provider: ^6.0.5 # Add this line for Provider package
  fluttertoast: ^8.0.9 # Use fluttertoast instead
  flutter_dotenv: ^6.0.1
  logger: ^2.7.0
  go_router: ^17.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
